<main
  data-vue="registrations-new"
  class="container"
>
  <h1 class="edit__title">サインイン</h1>
  <h2 class="edit__subtitle">
    各項目にご入力のうえ、送信ボタンを押してください。
  </h2>
  <div class="edit__wrapper offset-1 col-10">
    <%= form_for(resource, as: resource_name, url: registration_path(resource_name)) do |f| %>
    <%= render "devise/shared/error_messages", resource: resource %>
      <div class="edit__box form-group row justify-content-center">
        <div class="col-md-3 col-10 edit__text">メールアドレス</div>
        <div class="text-center col-md-8 col-10">
          <%= f.email_field :email, autofocus: true, autocomplete: "email", class: "col-12", "v-model": "email" %>
        </div>
      </div>
      <div class="edit__box form-group row justify-content-center">
        <div class="col-md-3 col-10 edit__text">お名前</div>
        <div class="text-center col-md-8 col-10">
          <%= f.text_field :name, autocomplete: "name", class: "col-12", "v-model": "name" %>
        </div>
      </div>
      <div class="edit__box form-group row justify-content-center">
        <div class="col-md-3 col-10 edit__text">パスワード</div>
        <div class="text-center col-md-8 col-10">
          <%= f.password_field :password, autocomplete: "new-password", class: "col-12", "v-model": "password" %>
          <div class="form-text text-muted edit__text">パスワードは<%= @minimum_password_length %>文字以上で入力してください。</div>
        </div>
      </div>
      <div class="edit__box form-group row justify-content-center">
        <div class="col-md-3 col-10 edit__text">パスワード確認</div>
        <div class="text-center col-md-8 col-10">
          <%= f.password_field :password_confirmation, autocomplete: "new-password", class: "col-12", "v-model": "passwordConfirmation"  %>
        </div>
      </div>
      <div class="edit__box text-center">
        <%= f.submit "送信する", class: "btn btn-primary font-weight-bold text-right",
             ":disabled": "!this.email || !this.name || !this.password || !this.passwordConfirmation" %>
      </div>
    <% end %>
  </div>
  <%= render "devise/shared/links" %>
</main>

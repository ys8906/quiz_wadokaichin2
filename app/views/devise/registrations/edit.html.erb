<main id="edit" class="container">
  <h1 class="edit__title">ユーザー情報変更</h1>
  <h2 class="edit__subtitle">
    変更する内容を入力し、送信してください。
    <br>変更しない内容は、空欄のままにしてください。
  </h2>
  <div class="edit__wrapper offset-1 col-10">
    <%= form_for(resource, as: resource_name, url: registration_path(resource_name), html: { method: :put }) do |f| %>
    <%= render "devise/shared/error_messages", resource: resource %>
      <div class="edit__box form-group row justify-content-center">
        <div class="col-md-3 col-10 edit__text">
          メールアドレス
          <span class="edit__text--notice">※必須</span>
        </div>
        <div class="text-center col-md-8 col-10">
          <%= f.email_field :email, autofocus: true, autocomplete: "email",
              class: "col-12", "v-model": "email" %>
        </div>
      </div>
      <div class="edit__box form-group row justify-content-center">
        <div class="col-md-3 col-10 edit__text">
          現在のパスワード
          <span class="edit__text--notice">※必須</span>
        </div>
        <div class="text-center col-md-8 col-10">
          <%= f.password_field :current_password, autocomplete: "current-password",
              class: "col-12", "v-model": "currentPassword" %>
        </div>
      </div>
      <div class="edit__box form-group row justify-content-center">
        <div class="col-md-3 col-10 edit__text">お名前</div>
        <div class="text-center col-md-8 col-10">
          <%= f.text_field :name, autocomplete: "name", class: "col-12" %>
        </div>
      </div>
      <div class="edit__box form-group row justify-content-center">
        <div class="col-md-3 col-10 edit__text">新しいパスワード</div>
        <div class="text-center col-md-8 col-10">
          <%= f.password_field :password, autocomplete: "new-password",
              class: "col-12" %>
          <div class="form-text text-muted edit__text">
            パスワードは<%= @minimum_password_length %>文字以上で入力してください。
          </div>
        </div>
      </div>
      <div class="edit__box form-group row justify-content-center">
        <div class="col-md-3 col-10 edit__text">新しいパスワード確認</div>
        <div class="text-center col-md-8 col-10">
          <%= f.password_field :password_confirmation, autocomplete: "new-password",
              class: "col-12"  %>
        </div>
      </div>
      <div class="edit__box text-center">
        <%= f.submit "送信する", class: "btn btn-primary font-weight-bold text-right",
             ":disabled": "!this.email || !this.currentPassword" %>
      </div>
    <% end %>
  </div>
</main>

<script>
new Vue ({
	el: '#edit',
	data: {
    email: null,
    currentPassword: null
  }
})
</script>